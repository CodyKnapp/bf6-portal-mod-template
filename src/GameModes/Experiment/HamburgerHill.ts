import { Core_AGameMode } from '../../Core/AGameMode';

export class HamburgerHill extends Core_AGameMode {
  // Initialize all capture points to 10 second capture / neutralize
  // Make middle spawn point 15 cap/neutralize
  // Adjust scoring so no points are scored unless the middle zone is held
  // Each death = tickets - number of zones held
}